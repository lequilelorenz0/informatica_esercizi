#include <stdio.h>
int MCD(int x,int y);
void semplifica(int x,int y,int z);
int main()
{ 
    //leggi i numeri 
    int num,num2,ris;
    printf("inserisci i due numeri");
    scanf("%d%d"&num,&num2);
    r=MCD(num,num2)
    semplifica(num,num2,ris);
    return 0;
}
int mcd(int x,int y);
{
    while(x=!y)
    if(x>y)
    {
        x=x-y;
    }
    else
    {
        y=y-x;

    }
    return x;
    void semplifica
}

